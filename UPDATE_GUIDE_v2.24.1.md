# GLOEUM v2.24.1 변경·검수 가이드

## 목적

v2.24.0 통합본(기업 바이어 + Instagram 인플루언서)의 실제 다운로드 결과 검수에서 발견된
두 가지 결함을 수정합니다. 신규 기능 추가는 없습니다.

## 수정 내역

### 1. `campaign_runner.py` 체크포인트 버전 고정 결함 (기업 바이어)

- **증상**: 세부업종을 여러 개 묶어 동시 수집(`industry_groups`)할 때 저장되는
  체크포인트의 `schema_version`이 실제 실행 버전과 무관하게 `"2.23.9"`로
  하드코딩되어 있었습니다. v2.23.10부터 v2.24.0까지 여러 버전에서 방치되어 있었습니다.
- **수정**: `version.py`의 `SCHEMA_VERSION`을 그대로 사용하도록 변경했습니다.
- **영향 범위**: 다업종 동시 수집을 중단 후 재개하는 경우에만 관련됩니다. 단일
  세부업종 수집에는 영향이 없었습니다.

### 2. Instagram 인플루언서 국가 필터 결함

- **증상**: 실제 다운로드 파일(베트남·뷰티, 20건) 검수 결과, A등급 상위 후보
  대다수가 베트남과 무관한 계정이었습니다(네덜란드, 미국, 멕시코, 독일 등).
  기존 로직은 검색결과 본문 어딘가에 국가명 문자열이 있으면 가점을 주는 방식이라,
  Brave 검색이 관련 없는 맥락에서 그 단어를 가져와도 통과했습니다.
- **수정**:
  - 국기 이모지 또는 도시명 같은 **명시적 위치 신호**로만 위치를 판단하도록 변경.
  - 대상국 신호가 없으면 `위치확인=미확인`, 다른 나라의 신호가 확인되면
    `위치확인=불일치`로 표시하고 감점·등급 강등(A 등급 불가).
  - **자동 제외는 하지 않습니다.** 사람이 최종 판단할 수 있도록 후보 목록에는
    남기되, 등급과 표시로 주의를 유도합니다.
- **부가 수정**: 기업·미디어 계정 제외 키워드에 `magazine`, `editorial`,
  `official page`, `official account`, `folio` 등을 추가. 팔로워 5만 이상이면서
  1인칭 표현이 전혀 없는 계정은 `브랜드·미디어 의심(수동확인)`으로 별도 표시.
- **Excel 변경**: `00_인플루언서결과` 시트에 `위치확인`, `위치근거` 컬럼 추가
  (국가 컬럼 바로 뒤).

## 재현 검증 (이번 검토에서 실행)

지난 다운로드 파일에서 문제였던 3개 계정을 동일한 소개글로 재구성해 수정된 로직에
직접 통과시켰습니다.

| 계정 | 수정 전 | 수정 후 |
|---|---|---|
| sokobeauty (네덜란드 소개) | A등급 84점 | **B등급 59점, 위치확인=불일치(Netherlands)** |
| mensfoliovietnam (패션 매거진) | A등급 통과 | **기업·공식상점 계정으로 제외** |
| someone_hanoi (베트남 국기 포함, 대조군) | — | **A등급 84점, 위치확인=확인(국기 일치)** — 정상 케이스는 그대로 통과 확인 |

엑셀 생성(`build_influencer_excel`)까지 오류 없이 끝까지 실행되는 것을 확인했습니다.

## 미실행 항목 (확인 필요)

- 이번 ZIP에는 `run_offline_tests.py`를 포함한 전체 테스트 스위트가 없어
  (배포 전용 최소 ZIP), **85/85 전체 회귀 테스트는 실행하지 못했습니다.**
  전체 소스 ZIP을 받으면 재검증이 필요합니다.
- 실제 Render 배포, 실서비스 데이터로의 재수집 결과는 검증되지 않았습니다.

## 1차 재검수 조건

| 항목 | 기준 |
|---|---|
| 대상 국가 | Vietnam |
| 분야 | 뷰티·화장품 |
| 후보 목표 | 20개 |
| A등급 중 위치확인=불일치 | 0건 |
| 명백한 기업·미디어 계정 | 0건 |
| 브랜드·미디어 의심 표시 건 | 수동으로 열어서 실제 여부 확인 |

이 기준을 통과한 뒤에만 100개로 확대하십시오.
